<!-- loop over hash upstream and assign values -->
upstream backend  {
  server <%= @server1 %>;
  server <%= @server2 %>;
  server <%= @server3 %>;
}

server {
        location / {
          proxy_pass  http://backend;
       }
}